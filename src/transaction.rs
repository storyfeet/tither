/*
use chrono::NaiveDate
use crate::money::Money;
use crate::action::Action;
use crate::error::ParseError;

pub struct Transaction {
    curr: String,
    date:NaiveDate,
    amount: Money,
    items:Vec<String>,
}

pub fn buildTransactions(aa:Vec<Action>)->Result<Vec<Transaction>,ParseError>{
    let mut def_year = None;
    let mut def_curr = None;
    let mut def_date = None;

}
*/
